//single source shortest path with non negative edge path
//O(e*log(v))
/**
 * Maintain "dist" - keeps track of shortest distance
 * of every node from starting node.
 * Priority Queue - key value pair HEAP
 */